default: true
MD013: false          # line length - not enforced
MD024:
  siblings_only: true # allow duplicate headings in different sections (CHANGELOG-friendly)
MD033: false          # inline HTML allowed (admonitions etc.)
MD041: false          # first line need not be h1 (frontmatter files)
