# Image Generation Dependencies
#
# SINGLE SOURCE OF TRUTH: pyproject.toml + uv.lock.
# This file is a convenience mirror of the *runtime* dependencies declared in
# pyproject.toml for tooling that consumes requirements.txt (e.g. plain
# `pip install -r requirements.txt`). The version ranges here MUST match
# pyproject.toml exactly. Prefer `uv sync` for development, which installs the
# fully pinned, hash-verified set from uv.lock.
#
# If you change a pin, change it in pyproject.toml first, then mirror it here.

# Google Gemini AI SDK for image generation
google-genai>=2.2.0,<2.3.0

# HTTP client for Topaz Labs API (post-processing enhancement)
httpx>=0.27.0,<1.0.0

# Settings management (BaseSettings reads GEMINI_API_KEY / TOPAZ_API_KEY)
pydantic-settings>=2.0.0,<3.0.0

# Structured logging for error / diagnostic paths.
# Upper bound on next major: structlog processor APIs historically change at
# majors, and silent log misconfiguration is the typical failure mode.
structlog>=24.0.0,<26.0.0

# Pinned to address CVE-2026-44431 (sensitive header leak on proxied low-level
# redirects) and CVE-2026-44432 (decompression-bomb safeguard bypass in the
# streaming API), both fixed in 2.7.0. urllib3 may be pulled transitively by
# other dependencies; the explicit pin ensures a patched version is resolved.
urllib3>=2.7.0,<3.0.0

# Pinned to address CVE-2026-45409. idna is pulled transitively (via httpx and
# the google-genai stack); the explicit floor ensures a patched version (3.15)
# is resolved regardless of what a transitive dependency requests.
idna>=3.15
