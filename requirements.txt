# Image Generation Dependencies
# Install with: pip install -r requirements.txt

# Google Gemini AI SDK for image generation
google-genai>=2.2.0,<2.3.0

# HTTP client for Topaz Labs API (post-processing enhancement)
httpx>=0.27.0,<1.0.0

# Settings management (BaseSettings reads GEMINI_API_KEY / TOPAZ_API_KEY)
pydantic-settings>=2.0.0,<3.0.0

# Structured logging for error / diagnostic paths
structlog>=24.0.0
